jdk:
  - openjdk11

before_install:
  - sdk install java 11.0.2-open
  - sdk use java 11.0.2-open
  - chmod +x ./gradlew

install:
  - ./gradlew :app:assembleRelease :app:androidSourcesJar

jdk_cache: true